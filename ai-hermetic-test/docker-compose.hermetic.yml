# Hermetic Server Docker Compose
# Service: payment-service
# Generated by TARS

version: '3.8'

services:
  payment-service-hermetic:
    build:
      context: .
      dockerfile: Dockerfile.hermetic
    image: hermetic-payment-service:latest
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=hermetic
      - JAVA_OPTS=-Xmx512m -Xms256m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
