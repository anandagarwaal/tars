# Hermetic Server Docker Compose
# Generated by TARS

version: '3.8'

services:
  demo-checkout-hermetic:
    build:
      context: .
      dockerfile: Dockerfile.hermetic
    image: hermetic-demo-checkout:latest
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=hermetic
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
